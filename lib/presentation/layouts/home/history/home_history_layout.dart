import 'package:flutter/material.dart';

class HomeHistoryLayout extends StatelessWidget {
  const HomeHistoryLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}